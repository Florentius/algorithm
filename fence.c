#include <stdio.h>

#define MAX(a,b) ((a>=b)? (a) : (b))

int calc(int p[], int left, int right)
{
	int ret;
	int mid = left+right/2;

	if(left == right) return p[left];
	ret = MAX(calc(

	
}

int main(void)
{
	/* int test_case; */
	/* int T; */
	/* setbuf(stdout, NULL); */
	/* scanf("%d", &T); */
	
	/* for (test_case = 1; test_case <= T; ++test_case) */
	/* { */

	/* } */
	int a[10] = {7,1,5,9,6,7,3};
	
	return 0;
}
